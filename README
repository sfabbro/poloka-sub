poloka-sub
===========

poloka is a software package to process astronomical
images. poloka-sub is the image subtraction module
of the poloka suite. It contains:

* spatially variable kernel fitting between two images
* production of subtracted image and simple point source detection

To install poloka-sub, see the INSTALL file.
